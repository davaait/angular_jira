import { BtnAttributeDirective } from './btn-attribute.directive';

describe('BtnAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
