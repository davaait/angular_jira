export enum TasksControls {
  name = "name",
  priority = "priority",
  dueDate = "dueDate",
  group = "group",
  description = "description"
}

export enum ListControl {
  name = "name",
  color = "color",
}
