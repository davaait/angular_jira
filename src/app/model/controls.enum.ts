export enum TasksControls {
  name = "name",
  priority = "priority",
  dueDate = "dueDate",
  group = "group",
  users = "users",
  description = "description",
}

export enum ListControl {
  name = "name",
  color = "color",
}

export enum BoardControl {
  name = "name",
  users = "users",
}
